import { Tag } from "@aleph-front/core";
import styled from "styled-components";
import tw from "twin.macro";

export const StyledTag = styled(Tag)`
  ${tw`w-fit whitespace-normal rounded-none bg-[#F1E9FD]`}
`;
